apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wisdom-ingress
spec:
  ingressClassName: nginx
  rules:
  - http: 
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: wisdom-service
            port:
              number: 80
